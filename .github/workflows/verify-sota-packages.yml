name: Verify sota packages
on:
  workflow_dispatch:
  pull_request:
    paths:
      - 'son-of-anton-cli/**'
      - 'son-of-anton-core/**'
      - '.github/workflows/verify-sota-packages.yml'
      - '.github/workflows/release-sota.yml'
      - 'scripts/staged-directory.mjs'
      - 'scripts/test/staged-directory.test.mjs'
permissions:
  contents: read
concurrency:
  group: sota-packages-${{ github.ref }}
  cancel-in-progress: true
jobs:
  package:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: macos-14
            target: macos-arm64
          - os: ubuntu-22.04
            target: linux-x64
          - os: windows-2022
            target: windows-x64
    runs-on: ${{ matrix.os }}
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
      - run: npm ci
        working-directory: son-of-anton-core
      - run: npm ci
        working-directory: son-of-anton-cli
      - name: Compile before testing
        shell: bash
        run: |
          npm --prefix son-of-anton-core run build
          npm --prefix son-of-anton-cli run build
      - name: Protocol, isolation and upgrade regressions
        shell: bash
        run: |
          node --test son-of-anton-core/dist/acp/*.test.js son-of-anton-core/dist/workspace/*.test.js
          node --test son-of-anton-cli/dist/test/updateInstall.test.js son-of-anton-cli/dist/test/updateDiscovery.test.js
          node --test son-of-anton-cli/scripts/lib/file-operations.test.mjs son-of-anton-cli/scripts/lib/signing.test.mjs son-of-anton-cli/scripts/lib/pipeline-safety.test.mjs son-of-anton-cli/scripts/lib/vendor-target.test.mjs son-of-anton-cli/scripts/lib/package-all.test.mjs scripts/test/staged-directory.test.mjs
      - name: Require a native runner for package smoke checks
        shell: bash
        env:
          EXPECTED_TARGET: ${{ matrix.target }}
        run: |
          node -e "const os = {darwin: 'macos', win32: 'windows', linux: 'linux'}[process.platform]; const actual = os + '-' + process.arch; if (actual !== process.env.EXPECTED_TARGET) throw new Error('Native smoke requires ' + process.env.EXPECTED_TARGET + '; runner is ' + actual)"
      - name: Build and verify installed binary
        run: npm run package:${{ matrix.target }}
        working-directory: son-of-anton-cli
