{
	"name": "@son-of-anton/embeddings",
	"version": "1.0.0",
	"description": "Shared embedding providers (Voyage, OpenAI-compatible, mock) for Son of Anton backend services",
	"main": "dist/index.js",
	"scripts": {
		"build": "tsc",
		"test": "node --test dist/test/*.js"
	},
	"devDependencies": {
		"@types/node": "^20.0.0",
		"typescript": "^5.6.0"
	}
}
