{
	"name": "@son-of-anton/walkthrough",
	"version": "0.1.0",
	"description": "Agent Action Walkthroughs — generates human-readable narratives explaining agent decisions",
	"main": "dist/src/index.js",
	"scripts": {
		"build": "tsc",
		"postbuild": "node -e \"const{cpSync}=require('fs');cpSync('_shared','dist/_shared',{recursive:true,force:true})\"",
		"start": "node dist/src/index.js",
		"dev": "tsc --watch & node --watch dist/src/index.js",
		"pretest": "npm run build",
		"test": "node --test dist/src/test/**/*.test.js"
	},
	"dependencies": {
		"express": "^4.18.2",
		"express-rate-limit": "^7.4.0"
	},
	"devDependencies": {
		"@types/express": "^4.17.21",
		"@types/node": "^20.11.0",
		"typescript": "^5.3.3"
	}
}
