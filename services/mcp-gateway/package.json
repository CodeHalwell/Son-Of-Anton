{
	"name": "soa-mcp-gateway",
	"version": "1.0.0",
	"description": "Son of Anton — MCP gateway exposing code graph and vector search tools",
	"main": "dist/index.js",
	"scripts": {
		"build": "node build.mjs",
		"typecheck": "tsc --noEmit",
		"start": "node dist/index.js",
		"dev": "node build.mjs --watch & nodemon dist/index.js",
		"test": "node build.mjs && node --test test/**/*.test.js",
		"lint": "eslint src/**/*.ts"
	},
	"dependencies": {
		"@modelcontextprotocol/sdk": "^1.12.0",
		"@qdrant/js-client-rest": "^1.12.0",
		"redis": "^4.7.0",
		"zod": "^3.23.0"
	},
	"devDependencies": {
		"@types/node": "^20.0.0",
		"esbuild": "^0.24.2",
		"typescript": "^5.6.0"
	},
	"private": true
}
