{
	"name": "soa-mcp-gateway",
	"version": "1.0.0",
	"description": "Son of Anton \u2014 MCP gateway exposing code graph and vector search tools",
	"main": "dist/index.js",
	"scripts": {
		"build": "npm run typecheck && node build.mjs",
		"typecheck": "tsc --noEmit",
		"start": "node dist/index.js",
		"dev": "node build.mjs --watch & nodemon dist/index.js",
		"test": "npm run build && node -e \"require('fs').rmSync('dist-test', {recursive:true, force:true})\" && tsc -p tsconfig.test.json && node -e \"for(const dir of ['_lib','_shared'])require('fs').cpSync(dir,'dist-test/'+dir,{recursive:true})\" && node --test test/*.test.js dist-test/test/*.test.js",
		"lint": "eslint src/**/*.ts"
	},
	"dependencies": {
		"@modelcontextprotocol/sdk": "^1.12.0",
		"@qdrant/js-client-rest": "^1.12.0",
		"redis": "^4.7.0",
		"zod": "^3.25.76"
	},
	"devDependencies": {
		"@types/node": "^20.0.0",
		"esbuild": "^0.24.2",
		"typescript": "^5.6.0"
	},
	"private": true
}
