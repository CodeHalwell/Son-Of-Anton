{
	"name": "soa-background-tasks",
	"version": "1.0.0",
	"description": "Son of Anton \u2014 Background Task Manager service",
	"main": "dist/index.js",
	"scripts": {
		"build": "tsc",
		"start": "node dist/index.js",
		"dev": "tsc --watch & nodemon dist/index.js",
		"test": "npm run build && node -e \"require('fs').rmSync('dist-test', {recursive:true, force:true})\" && tsc -p tsconfig.test.json && node --test test/http/*.test.js dist-test/test/*.test.js"
	},
	"dependencies": {
		"dockerode": "^4.0.0"
	},
	"devDependencies": {
		"@types/dockerode": "^3.3.0",
		"@types/node": "^20.0.0",
		"typescript": "^5.6.0"
	},
	"private": true
}
