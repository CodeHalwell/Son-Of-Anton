{
	"name": "@son-of-anton/sanitiser",
	"version": "1.0.0",
	"description": "Shared prompt-injection sanitiser core for Son of Anton backend services",
	"main": "dist/index.js",
	"scripts": {
		"build": "tsc",
		"test": "node --test dist/test/*.js"
	},
	"devDependencies": {
		"@types/node": "^20.0.0",
		"typescript": "^5.6.0"
	}
}
