# Live chat rendering check — September 7, 2026

Two real Claude Code subscription responses completed in the rebuilt native Son of Anton app, using a disposable workspace containing a README and a three-line TypeScript clamp example. The user explicitly approved trust for that test folder. No project source was supplied to the model and no source-file edits were requested from the agent.

## Findings and fixes

- Markdown tables were rendered as pipe-delimited text. Tables now render semantic headers/cells, inline code and links, with horizontal scrolling in narrow chat panes.
- Streaming displayed raw Markdown and briefly exposed the follow-up protocol payload. Streaming now renders Markdown per animation frame, preserves the raw source for completion, closes unfinished code fences for display, and hides split follow-up markers.
- The thinking indicator remained after response text arrived. It now clears on the first nonempty token.
- The footer estimated subscription responses using generic API prices while the session meter showed zero. Both now use the same model-aware cost source, and completed messages refresh the transcript meter.
- ACP responses now explicitly display unavailable usage instead of presenting missing token/billing reports as zero.
- The trust dialog displayed two Cancel buttons. Its dismissal action is now explicit; temporary workspace trust also updates the status bar.

## Verification

Extension TypeScript and gulp compilation passed. The final review has 383 extension unit tests and 24 browser scenarios passing, including the final block-layout adjustment. Browser checks include split stream markers, partially streamed code, preserved tool cards, narrow table layouts, escaped HTML, and unavailable ACP usage.

The first live response reported that the legacy Claude Code model route did not expose filesystem tools. The second response used the example pasted into the conversation and exercised code blocks, a documentation link and an edge-case table. Its initial prose included a contradictory edge-case explanation; this check establishes UI behavior, not model answer correctness.

A separate authenticated ACP prompt succeeded through the rebuilt `sota acp --agent anton-docs` CLI and the existing Claude Code subscription. The shared runtime received streamed response text and `end_turn`, reporting one completed turn and no failed turns. Shutdown then reported zero processes, active requests and queued requests, and the check exited successfully. This checks the real model transport, separately from the native UI.

The Mac was subsequently available for the native review. The prepared Anton Docs ACP route completed in the real sidebar, including heading/table/code/link rendering, streaming, completion, and unavailable usage. Code Open created an untitled TypeScript editor with the exact snippet. The authorized trust scope remained the disposable workspace and was granted temporarily after reload.

## Sidebar follow-up

The sidebar uses the same `ChatSession` renderer through `ChatViewProvider`. A dedicated browser check exercised populated conversations at 360×620, 280×480 and 420×540 pixels, including a restored table/code/link response, mixed reported and unavailable usage, model/agent/cost menus, streaming and cancellation. It found a gap above the sticky task header that exposed scrolled text, message actions sharing the last link's line, and excess blank rows around fenced code. These are fixed; code blocks also use one enclosing border.

The regression checks the pinned header against the transcript edge, action placement, block spacing, available transcript height, control/menu bounds, and host cancellation. The final suite contains 24 browser scenarios. Screenshots under the task's `ui/full-review` artifact directory are browser fixtures; native inspection used the running isolated app separately. The earlier locked-Mac limitation is resolved. The [full review](full-ui-review-2026-09-07.md) records the expanded native coverage and backend checks.

The final pass also repaired restored composer routing: conversation metadata stored Anton Docs correctly, but the webview reset its active agent to Anton. Restoring now selects the saved specialist and retains individual message authors and unavailable-usage markers. A later live request uncovered excessive inherited Claude CLI context; the model transport now isolates that catalog, and a real authenticated retry succeeded with 260 input tokens.

The subsequent native ACP request completed successfully: a heading, two table rows, and a TypeScript guard rendered in the sidebar, attributed to Anton Docs, with unavailable usage shown consistently. A final composer correction replaces the ineffective local model picker with Managed by ACP for adapter-owned specialists; the native model picker returns when selecting an ordinary specialist.
